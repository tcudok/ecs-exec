{
  "name": "@tcudok/ecs-exec",
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/exec",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "release": "yarn clean && yarn build && yarn publish"
  },
  "dependencies": {
    "aws-sdk": "^2.573.0",
    "debug": "^4.1.1"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/node": "^12",
    "prettier": "^1.19.1",
    "ts-node": "^8.5.2",
    "typescript": "^3.6.4"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  }
}
