{
  "name": "@tcudok/ecs-exec",
  "version": "1.0.1",
  "license": "Unlicense",
  "repository": "github:tcudok/ecs-exec",
  "main": "dist/exec",
  "types": "dist/exec.d.ts",
  "engines": {
    "node": ">=12"
  },
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "release": "yarn clean && yarn build && yarn publish"
  },
  "dependencies": {
    "aws-sdk": "^2.573.0",
    "debug": "^4.1.1"
  },
  "devDependencies": {
    "@types/chalk": "^2.2.0",
    "@types/debug": "^4.1.5",
    "@types/node": "^12",
    "chalk": "^3.0.0",
    "prettier": "^1.19.1",
    "randoma": "^1.3.0",
    "ts-node": "^8.5.2",
    "typescript": "^3.6.4"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  }
}
